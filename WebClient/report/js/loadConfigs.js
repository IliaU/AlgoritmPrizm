const HOSTNAME  = "http://RUGGMOOFPRISM1";
const UTIL_HOSTNAME = "http://RUGGMOOFPRISM1:5000";

const PRISM_SESSION = JSON.parse(sessionStorage.getItem("session"));

const CONFIG = {
    autoLegal: true,
};

var session = JSON.parse(sessionStorage.getItem("session"));;